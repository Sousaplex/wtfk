# Core LangChain dependencies for AI analysis
langchain>=0.1.0
langchain-google-genai>=1.0.0
python-dotenv>=1.0.0

# Visualization and data analysis dependencies
matplotlib>=3.7.0
seaborn>=0.12.0
networkx>=3.0
pandas>=2.0.0
numpy>=1.24.0

# PDF Generation (requires external tool)
# Install pandoc separately: https://pandoc.org/installing.html
# On macOS: brew install pandoc
# On Ubuntu/Debian: sudo apt-get install pandoc
# On Windows: winget install pandoc or download from website 